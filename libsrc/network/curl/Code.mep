module CurlCode
{
  var  const @CURLE_OK                      = 0;
  var  const @CURLE_UNSUPPORTED_PROTOCOL    =1;
  var  const @CURLE_FAILED_INIT             =2;
  var  const @CURLE_URL_MALFORMAT           =3;
  var  const @CURLE_NOT_BUILT_IN            =4;
  var  const @CURLE_COULDNT_RESOLVE_PROXY   =5;
  var  const @CURLE_COULDNT_RESOLVE_HOST    =6;
  var  const @CURLE_COULDNT_CONNECT         =7;
  var  const @CURLE_FTP_WEIRD_SERVER_REPLY  =8;
  var  const @CURLE_REMOTE_ACCESS_DENIED    =9;
  var  const @CURLE_FTP_ACCEPT_FAILED       =10;
  var  const @CURLE_FTP_WEIRD_PASS_REPLY    =11;
  var  const @CURLE_FTP_ACCEPT_TIMEOUT      =12;
  var  const @CURLE_FTP_WEIRD_PASV_REPLY    =13;
  var  const @CURLE_FTP_WEIRD_227_FORMAT    =14;
  var  const @CURLE_FTP_CANT_GET_HOST       =15;
  var  const @CURLE_OBSOLETE16              =16;
  var  const @CURLE_FTP_COULDNT_SET_TYPE    =17;
  var  const @CURLE_PARTIAL_FILE            =18;
  var  const @CURLE_FTP_COULDNT_RETR_FILE   =19;
  var  const @CURLE_OBSOLETE20              =20;
  var  const @CURLE_QUOTE_ERROR             =21;
  var  const @CURLE_HTTP_RETURNED_ERROR     =22;
  var  const @CURLE_WRITE_ERROR             =23;
  var  const @CURLE_OBSOLETE24              =24;
  var  const @CURLE_UPLOAD_FAILED           =25;
  var  const @CURLE_READ_ERROR              =26;
  var  const @CURLE_OUT_OF_MEMORY           =27;
  var  const @CURLE_OPERATION_TIMEDOUT      =28;
  var  const @CURLE_OBSOLETE29              =29;
  var  const @CURLE_FTP_PORT_FAILED         =30;
  var  const @CURLE_FTP_COULDNT_USE_REST    =31;
  var  const @CURLE_OBSOLETE32              =32;
  var  const @CURLE_RANGE_ERROR             =33;
  var  const @CURLE_HTTP_POST_ERROR         =34;
  var  const @CURLE_SSL_CONNECT_ERROR       =35;
  var  const @CURLE_BAD_DOWNLOAD_RESUME     =36;
  var  const @CURLE_FILE_COULDNT_READ_FILE  =37;
  var  const @CURLE_LDAP_CANNOT_BIND        =38;
  var  const @CURLE_LDAP_SEARCH_FAILED      =39;
  var  const @CURLE_OBSOLETE40              =40;
  var  const @CURLE_FUNCTION_NOT_FOUND      =41;
  var  const @CURLE_ABORTED_BY_CALLBACK     =42;
  var  const @CURLE_BAD_FUNCTION_ARGUMENT   =43;
  var  const @CURLE_OBSOLETE44              =44;
  var  const @CURLE_INTERFACE_FAILED        =45;
  var  const @CURLE_OBSOLETE46              =46;
  var  const @CURLE_TOO_MANY_REDIRECTS      =47;
  var  const @CURLE_UNKNOWN_OPTION          =48;
  var  const @CURLE_TELNET_OPTION_SYNTAX    =49;
  var  const @CURLE_OBSOLETE50              =50;
  var  const @CURLE_PEER_FAILED_VERIFICATION= 51;
  var  const @CURLE_GOT_NOTHING             =52;
  var  const @CURLE_SSL_ENGINE_NOTFOUND     =53;
  var  const @CURLE_SSL_ENGINE_SETFAILED    =54;
  var  const @CURLE_SEND_ERROR              =55;
  var  const @CURLE_RECV_ERROR              =56;
  var  const @CURLE_OBSOLETE57              =57;
  var  const @CURLE_SSL_CERTPROBLEM         =58;
  var  const @CURLE_SSL_CIPHER              =59;
  var  const @CURLE_SSL_CACERT              =60;
  var  const @CURLE_BAD_CONTENT_ENCODING    =61;
  var  const @CURLE_LDAP_INVALID_URL        =62;
  var  const @CURLE_FILESIZE_EXCEEDED       =63;
  var  const @CURLE_USE_SSL_FAILED          =64;
  var  const @CURLE_SEND_FAIL_REWIND        =65;
  var  const @CURLE_SSL_ENGINE_INITFAILED   =66;
  var  const @CURLE_LOGIN_DENIED            =67;
  var  const @CURLE_TFTP_NOTFOUND           =68;
  var  const @CURLE_TFTP_PERM               =69;
  var  const @CURLE_REMOTE_DISK_FULL        =70;
  var  const @CURLE_TFTP_ILLEGAL            =71;
  var  const @CURLE_TFTP_UNKNOWNID          =72;
  var  const @CURLE_REMOTE_FILE_EXISTS      =73;
  var  const @CURLE_TFTP_NOSUCHUSER         =74;
  var  const @CURLE_CONV_FAILED             =75;
  var  const @CURLE_CONV_REQD               =76;
  var  const @CURLE_SSL_CACERT_BADFILE      =77;
  var  const @CURLE_REMOTE_FILE_NOT_FOUND   =78;
  var  const @CURLE_SSH                     =79;
  var  const @CURLE_SSL_SHUTDOWN_FAILED     =80;
  var  const @CURLE_AGAIN                   =81;
  var  const @CURLE_SSL_CRL_BADFILE         =82;
  var  const @CURLE_SSL_ISSUER_ERROR        =83;
  var  const @CURLE_FTP_PRET_FAILED         =84;
  var  const @CURLE_RTSP_CSEQ_ERROR         =85;
  var  const @CURLE_RTSP_SESSION_ERROR      =86;
  var  const @CURLE_FTP_BAD_FILE_LIST       =87;
  var  const @CURLE_CHUNK_FAILED            =88;
  var  const @CURLE_NO_CONNECTION_AVAILABLE =89;
}