import console;
use Console;
_mmain:$a,$c
{	
	var $i=0,const $inst = mmrt(Console);
	while($i<100)
	{
		if($i<1)
		{
			$inst.Out("menthol");
			$i+=1;
			continue;
		}

		$i = $i+1;
		$inst.Out($i);
	}
}
