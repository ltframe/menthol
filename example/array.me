import console;
use Console;
_mmain:$a,$c
{			
	var $arr = [1,2,3,4,5,6];
	$a = $arr[1..];
	for(var $i in $a){
		Console.Out($i);
	}
	Console.Out("************");
	$a = $arr[..3];
	for(var $i in $a){
		Console.Out($i);
	}
	Console.Out("************");
	$a = $arr[2..5];
	for(var $i in $a){
		Console.Out($i);
	}

	Console.Out("************");
	$arr = "abcdefghi";
	Console.Out($arr[1]);
}