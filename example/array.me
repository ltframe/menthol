import console;
use Console;
_mmain:$a,$c
{			
	var const $inst =mmrt(Console);
	var $arr = [1,2,3,4,5,6];
	$a = $arr[1..];
	for(var $i in $a){
		$inst.Out($i);
	}
	$inst.Out("************");
	$a = $arr[..3];
	for(var $i in $a){
		$inst.Out($i);
	}
	$inst.Out("************");
	$a = $arr[2..5];
	for(var $i in $a){
		$inst.Out($i);
	}

	$inst.Out("************");
	$arr = "abcdefghi";
	$inst.Out($arr[1]);
}