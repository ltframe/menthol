import console;
use Console;
_mmain:$a,$c
{	
	var $inst =mmrt(Console);
	$inst.Out(2+1);
	$inst.Out(2-1);
	$inst.Out(-2+1);
	$inst.Out(-2-1);
	$inst.Out(-2*1);
	$inst.Out(2/1);	
	$inst.Out(3%2);
	$inst.Out(3**2);
}
