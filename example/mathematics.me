import console;
use Console;
_mmain:$a,$c
{	
	Console.Out(2+1);
	Console.Out(2-1);
	Console.Out(-2+1);
	Console.Out(-2-1);
	Console.Out(-2*1);
	Console.Out(2/1);	
	Console.Out(3%2);
	Console.Out(3**2);
}
