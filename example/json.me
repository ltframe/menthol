import console;
import string;
use Console,Json;
_mmain:$a,$c
{	
		
		var $s ="{\"name\":\"fengxin\",\"passwd\":\"123\",\"num\":[{\"rrr\":\"ffffffffff\"}]}";
		var $j = Json.JsonParse($s);
		Console.Out(Json.GetObjectItem($j,"name"));Console.Out(Json.GetObjectItem($j,"passwd"));
		var $m = Json.GetObjectItem($j,"num");
		var $count = Json.GetArraySize($m);
		var $ar = Json.GetArrayItem($m,0);	
		Console.Out(Json.Type($m));
		Console.Out(Json.GetObjectItem($ar,"rrr"));
}
